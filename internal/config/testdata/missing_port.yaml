server:
  host: localhost
  timeout: 10

keycloak:
  url: https://auth.example.com
  realm: test-realm
  client_id: test-client

session:
  secret: this-is-a-test-secret-with-at-least-32-characters-for-validation
